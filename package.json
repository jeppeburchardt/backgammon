{
  "name": "bg2",
  "version": "1.0.0",
  "description": "",
  "main": "./distribution/index.js",
  "scripts": {
    "demo": "",
    "build": "babel source --presets babel-preset-es2015 --out-dir distribution",
    "test": "mocha --require babel-polyfill --compilers js:babel-register",
    "coverage": "nyc --reporter=text mocha --require babel-polyfill --compilers js:babel-register",
    "coverage2": "nyc --reporter=lcov --reporter=text mocha --require babel-polyfill --compilers js:babel-register",
    "dev": "mocha --watch --require babel-polyfill --compilers js:babel-register"
  },
  "author": "Jeppe Burchardt",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-istanbul": "^4.1.5",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "cross-env": "^5.0.5",
    "mocha": "^3.5.3",
    "nyc": "^11.2.1"
  }
}
